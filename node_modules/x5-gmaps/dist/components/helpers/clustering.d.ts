import { X5ClusterGroup, X5ClusterItem, X5Pos } from 'src/types/x5gmaps';
export declare const getAveragePosition: (items: X5Pos[]) => {
    lat: number;
    lng: number;
};
export declare const organiseClusters: (items: X5ClusterItem[], zoom: number, maxZoom: number, tileSize: number) => Record<string, X5ClusterGroup>;
